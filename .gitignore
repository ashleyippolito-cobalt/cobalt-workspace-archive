.DS_Store
.claude/settings.local.json

# Sibling product repo clones — real code lives in their own GitHub repos,
# not tracked as part of this archive repo.
/SALTRoadmap/
/internal-tools/
/spice/
/spice-rack/
/salt/
/cobalt-release-plan-web/

# macOS custom-folder-icon resource-fork artifact
Icon?

# Auto-generated Codex-compatible mirror of .claude/skills + .claude/agents
# (lets Ashley use the same domain-expert agents/skills from Codex) — derived
# content, not a source of truth, don't track.
/.agents/
/.codex/
/AGENTS.md
